import 'tests/config/global.config'

describe('ROUTER - Buyer controller routes ', () => {
  test('Route should return json content type', async () => {
    const resp = 1
    expect(resp).toBe(1)
  })
})
